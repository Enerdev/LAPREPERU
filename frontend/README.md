
  # Dashboard aula virtual (Community)

  This is a code bundle for Dashboard aula virtual (Community). The original project is available at https://www.figma.com/design/66M6AnH6yfMqomTIZkm39y/Dashboard-aula-virtual--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  